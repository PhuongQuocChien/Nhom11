@model IEnumerable<BaiTapNhom11.Models.GioiTinh>

@{
    ViewData["Title"] = "Index";
}

<h1>Chào mừng đến với trang giới tính cửa hàng sách BookKing</h1>

<p>
    <a asp-action="Create">Thêm mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenGioiTinh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenGioiTinh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaGioiTinh">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.MaGioiTinh">Xem</a> |
                <a asp-action="Delete" asp-route-id="@item.MaGioiTinh">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
