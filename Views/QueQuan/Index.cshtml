@model IEnumerable<BaiTapNhom11.Models.QueQuan>

@{
    ViewData["Title"] = "Index";
}

<h1>Chào mừng đến với trang quê quán cửa hàng sách BookKing</h1>

<p>
    <a asp-action="Create">Thêm mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenQueQuan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenQueQuan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaQueQuan">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.MaQueQuan">Xem</a> |
                <a asp-action="Delete" asp-route-id="@item.MaQueQuan">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
